# ==========================================
# CONFIGURAÇÃO DA APLICAÇÃO - TASK MANAGER
# ==========================================
# Copie este ficheiro para .env e preencha os valores

# ==========================================
# AMBIENTE
# ==========================================
FLASK_ENV=production
# Valores: development | production

# ==========================================
# SEGURANÇA - KEYS SECRETAS
# ==========================================
# IMPORTANTE: Gere valores aleatórios seguros!
# Use: python -c "import secrets; print(secrets.token_hex(32))"

SECRET_KEY=your-super-secret-key-change-this-in-production
# Chave secreta para Flask (sessões, cookies)
# Mínimo 32 caracteres aleatórios

JWT_SECRET_KEY=your-jwt-secret-key-change-this-in-production
# Chave para assinar tokens JWT
# Mínimo 32 caracteres aleatórios

JWT_ACCESS_TOKEN_EXPIRES=30
# Tempo de expiração do token em minutos
# Recomendado: 15-60 minutos

# ==========================================
# BASE DE DADOS - POSTGRESQL
# ==========================================
DATABASE_URL=postgresql://user:password@localhost:5432/taskmanager
# Formato: postgresql://utilizador:password@host:porta/nome_bd
# Render fornece automaticamente esta variável

# Exemplo para desenvolvimento local:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/taskmanager_dev

# ==========================================
# CORS - ORIGENS PERMITIDAS
# ==========================================
CORS_ORIGINS=http://localhost:4200,https://seu-frontend.vercel.app
# Lista separada por vírgulas dos domínios permitidos
# Atualize com o URL do frontend em produção

# ==========================================
# RATE LIMITING
# ==========================================
RATELIMIT_ENABLED=true
# Ativar proteção contra abuso de API
# Valores: true | false

RATELIMIT_DEFAULT=100 per hour
# Limite padrão de requests
# Formatos: "100 per hour", "10 per minute", "1000 per day"

# ==========================================
# SERVIDOR
# ==========================================
PORT=5000
# Porta da aplicação (Render define automaticamente)

# ==========================================
# GUNICORN (Produção)
# ==========================================
GUNICORN_WORKERS=2
# Número de workers (não alterar no Render free tier)

GUNICORN_THREADS=2
# Threads por worker

LOG_LEVEL=info
# Nível de logging: debug | info | warning | error | critical

# ==========================================
# NOTAS IMPORTANTES
# ==========================================
# 1. NUNCA commite o ficheiro .env para o Git
# 2. No Render, defina estas variáveis na dashboard
# 3. Gere SECRET_KEY e JWT_SECRET_KEY únicos e seguros
# 4. Atualize CORS_ORIGINS após deploy do frontend
# 5. DATABASE_URL é fornecido automaticamente pelo Render

