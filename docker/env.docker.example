# ==========================================
# DOCKER COMPOSE - VARIÁVEIS DE AMBIENTE
# ==========================================
# Copie este ficheiro para .env na pasta docker/
# Uso: docker-compose --env-file docker/.env up

# ==========================================
# POSTGRESQL
# ==========================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=taskmanager_dev
POSTGRES_PORT=5432

# ==========================================
# PORTAS DOS SERVIÇOS
# ==========================================
# Altere se tiver conflitos de portas

# Backend Flask
BACKEND_PORT=5000

# Frontend Angular
FRONTEND_PORT=4200

# ==========================================
# NOTAS
# ==========================================
# 1. Estes são valores para DESENVOLVIMENTO LOCAL apenas
# 2. NÃO use estas passwords em produção
# 3. As portas podem ser alteradas se já estiverem em uso
# 4. O ficheiro .env não deve ser commitado ao Git

