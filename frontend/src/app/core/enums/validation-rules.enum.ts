/**
 * Enum para regras de validação
 */
export enum ValidationRules {
  USERNAME_MIN_LENGTH = 3,
  USERNAME_MAX_LENGTH = 80,
  PASSWORD_MIN_LENGTH = 6,
  TASK_TITLE_MAX_LENGTH = 200,
  DESCRIPTION_TRUNCATE_LENGTH = 100
}

